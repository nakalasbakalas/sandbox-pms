services:
  - type: web
    name: sandbox-hotel-pms
    runtime: python
    rootDir: sandbox_pms_mvp
    region: oregon
    plan: starter
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    preDeployCommand: flask --app app db upgrade
    startCommand: gunicorn --workers ${WEB_CONCURRENCY:-2} --bind 0.0.0.0:${PORT:-10000} --timeout 120 --worker-class sync --access-logfile - --error-logfile - app:app
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.11"
      - key: APP_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: sandbox-hotel-pms-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: AUTH_ENCRYPTION_KEY
        sync: false
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: FORCE_HTTPS
        value: "1"
      - key: TRUST_PROXY_COUNT
        value: "1"
      - key: AUTH_COOKIE_SECURE
        value: "1"
      - key: SESSION_COOKIE_SECURE
        value: "1"
      - key: AUTH_SHOW_RESET_LINKS
        value: "0"
      - key: AUTO_BOOTSTRAP_SCHEMA
        value: "0"
      - key: AUTO_SEED_REFERENCE_DATA
        value: "0"
      - key: LOG_LEVEL
        value: INFO
      - key: WEB_CONCURRENCY
        value: "2"
      - key: PAYMENT_PROVIDER
        value: disabled

databases:
  - name: sandbox-hotel-pms-db
    plan: basic-256mb
    region: oregon
    databaseName: sandbox_hotel_pms
    user: sandbox_hotel_pms
    postgresMajorVersion: "17"
