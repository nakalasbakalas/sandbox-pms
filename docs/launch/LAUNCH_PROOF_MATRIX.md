# Launch Proof Matrix

Status date: 2026-07-04.

Verdict: not launch-signed-off. Slice 5BI adds and tests a secret-safe Gmail OAuth handoff helper, but Render Gmail OAuth is still owner-gated and production Booking Email capture/backfill is not proven. Slice 5BH remains the latest public live deploy proof for Render deploy `dep-d93ud5nlk1mc73a2sbv0`; it confirms the deployed public-edge-helper commit `0de2eb3d612a555dbd6cac92948becd16aa24cae` remains live and healthy on the public Render service. Slice 5BG syncs the open GitHub launch issues with current evidence, but owner/provider proof is still missing for production auth/RBAC, room inventory source, workflow acceptance, secret inventory, recovery ownership, latest recovery point, WAF/rate-limit rules, and booking-email OAuth/backfill if required. This matrix separates local proof from open production/account-owner proof. Do not treat this file as proof by itself; each row must link to command output, redacted provider metadata, or manual acceptance evidence.

## P0 Matrix

| Area | Status | Current Evidence | Evidence Required To Close |
| --- | --- | --- | --- |
| Launch packet, index, and evidence command | Green locally | `LAUNCH_CHECKLIST.md`; `docs/launch/CODEX_LAUNCH_FINISH_PACKET.md`; this matrix; `docs/launch/CURRENT_STATUS_INDEX.md`; `docs/launch/evidence/2026-07-03-slice-5ay-housekeeping-sync.md`; `scripts/collect-launch-evidence.mjs` from `origin/main`; and local `npm.cmd run launch:evidence` through `scripts/launch-evidence.mjs`. | Keep docs current after each slice without converting local proof into production/account-owner sign-off. |
| GitHub launch issue tracker | Synced; launch issues remain open | `docs/launch/evidence/2026-07-03-slice-5bg-github-issue-sync.md` records current status comments posted to issues `#136`, `#137`, `#138`, `#140`, and `#142` after the public-edge-helper deploy and docs sync. | Keep issue status aligned with evidence. Do not close an issue until its acceptance criteria are met by current proof or explicit owner accepted-risk sign-off. |
| Current-checkout validation gate | Green locally for focused Slice 5BI checks; latest remote CI green for prior docs/status commit | `docs/launch/evidence/LAUNCH_GATE_RESULTS.md`; `docs/launch/evidence/2026-07-04-slice-5bi-gmail-oauth-handoff-helper.md`; `docs/launch/evidence/2026-07-04-slice-5bh-live-gmail-refresh.md`. Slice 5BI local checks passed for fixture consent URL generation, `npm.cmd test`, `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd run launch:evidence`, and `git diff --check`; Slice 5BH records GitHub Actions run `28674129355` passing `Install, test, build, and launch-check` for commit `1d2ea176b5759e98f30d038a8f3985ab299105af`, plus public-edge proof, live readiness, production preflight, and redacted Render Gmail OAuth status. Earlier Slice 5AT gate attempts exposed a tool timeout and then a non-mutating E2E role-access timeout before final passes; no gate was weakened. | Rerun before release if code, dependencies, migrations, or environment assumptions change. Monitor the observed non-mutating E2E latency/flakiness if it recurs. |
| Baseline validation ladder | Green locally | `docs/launch/evidence/2026-07-03-slice-5ay-housekeeping-sync.md` records passing merge-marker scan, `git diff --check`, `npm.cmd run launch:evidence`, `npm.cmd run db:doctor`, `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd test`, `npm.cmd run build`, `npm.cmd run render:validate`, `npm.cmd run live:check`, and `npm.cmd run launch:check` in the current checkout. | Rerun before release if code, dependency, environment, Render Blueprint, or live-readiness assumptions change. |
| DB-mutating E2E posture | Green locally with caveat | `docs/launch/evidence/DB_E2E_POSTURE.md`; `docs/launch/evidence/2026-07-03-slice-5ay-housekeeping-sync.md`; `docs/launch/evidence/2026-07-03-slice-5aj-core-workflow-local-db-refresh.md`; `docs/launch/evidence/2026-07-03-slice-5aa-db-e2e-posture-refresh.md`; `docs/launch/evidence/2026-07-02-slice-1-db-e2e.md`. Slice 5AY passed guarded `db:e2e:ready` and `test:e2e:db` with `ALLOW_DB_E2E=true` against the local disposable `sandbox_hotel_e2e` database. | Keep proof tied to disposable/staging `E2E_DATABASE_URL`; never production. If owner requires staging acceptance, rerun against an approved staging database and record redacted evidence. |
| Live unauthenticated protected API/page denial | Representative endpoints/pages green | `docs/launch/evidence/2026-07-03-slice-5ai-auth-rbac-unauth-refresh.md`; `docs/launch/evidence/2026-07-02-slice-2-auth-unauthenticated.md`; `docs/launch/evidence/AUTH_RBAC_PROOF.md`; `docs/launch/evidence/2026-07-02-slice-5z-live-protected-page-gate.md` refreshed live unauthenticated protected API and protected-page probes against `https://book.sandboxhotel.com`. | Add credentialed production role matrix and underprivileged-role denial proof before sign-off. |
| Live first-run setup completion gate | Green on current public deploy | Slice 5BH confirms Render live deploy `dep-d93ud5nlk1mc73a2sbv0`, commit `0de2eb3d612a555dbd6cac92948becd16aa24cae`, and public unauthenticated setup-complete still returns `403 Public first-run setup is disabled in production. Seed an admin user or configure INITIAL_SETUP_TOKEN.` See `docs/launch/evidence/2026-07-04-slice-5bh-live-gmail-refresh.md`; historical reprobe attempts remain in the Slice 5AB/5AH/5AK/5AN/5AQ/5AU/5AW/5AY/5AZ evidence files. | Rerun the public setup-complete reprobe after future deploys that touch setup/auth routes. |
| Production users/auth/RBAC/logout | Partial; credentialed production proof open | `docs/launch/evidence/AUTH_RBAC_PROOF.md` and `docs/launch/evidence/2026-07-03-slice-5ai-auth-rbac-unauth-refresh.md` record local username-first auth/RBAC tests, non-mutating E2E browser/contract smoke, live unauthenticated protected API denial, and representative live protected-page login gating. Slice 5AI notes the first non-mutating E2E attempt timed out but the retry passed after cleaning up orphaned validation processes. | Approved production user list, credentialed production login/logout proof, production role matrix, protected page denial for an underprivileged role, protected API mutation denial for an underprivileged role, and bootstrap/setup-token removal or rotation evidence. |
| Real production room inventory | Partial; aggregate counts recorded, owner/source proof open | `docs/launch/evidence/ROOM_INVENTORY_PROOF.md` and `docs/launch/evidence/2026-07-03-slice-5ba-room-inventory-proof-helper.md` record the canonical inventory proof state. Slice 5BA deployed `527e231...` to Render deploy `dep-d93pe7hkh4rs73dp5bcg` and ran one-off job `job-d93pfr6q1p3s73a2ufh0`, which returned counts-only production proof: `33` total rooms, `33` operationally available, `0` inactive, two redacted room-type buckets (`17` and `16`), and current statuses `VACANT_CLEAN=32`, `OCCUPIED_CLEAN=1`. The output omitted room numbers, guests, reservations, users, payments, and raw database URLs. Deploy logs show prod-safe seed skipped room inventory. | Owner/import evidence proving these aggregate rows came from the approved real room source and not fake seed/demo data; room-type labels only if operations owner approves exposing them. |
| Core hotel workflow acceptance | Partial local proof refreshed; staging/production-like acceptance open | `docs/launch/evidence/HOTEL_WORKFLOW_PROOF.md`, `docs/launch/evidence/DB_E2E_POSTURE.md`, and `docs/launch/evidence/2026-07-03-slice-5ay-housekeeping-sync.md` record guarded local `db:e2e:ready` and guarded local `test:e2e:db` passes against `localhost:55432/sandbox_hotel_e2e`. | Staging or controlled production-like/manual evidence for reservation creation/update/cancel, invalid date rejection, room assignment safety, check-in/out, payment/folio, housekeeping, and audit/timeline entries. |
| Secret hygiene and live secret evidence | Partial/open | Runtime behavior proves a session secret exists; local `launch:evidence` scans remain green; `docs/launch/evidence/SECRETS_AND_RECOVERY_PROOF.md`, `docs/launch/evidence/WAF_PROVIDER_POSTURE.md`, and `docs/launch/evidence/2026-07-04-slice-5bh-live-gmail-refresh.md` record safe Render metadata and the boundary that the available Render CLI path does not expose full secret key inventory or rotation timestamps. Slice 5BH checks booking-email Gmail OAuth key presence with the reusable status command and finds all supported credential paths not ready. Slice 5BI adds an owner-run Gmail OAuth code-exchange/apply helper that is tested for redaction, but no real values were accessed or applied. | Redacted Render secret key inventory, rotation dates where available, cleanup decision for legacy key names, and owner confirmation. |
| Recovery, rollback, and WAF ownership | Partial/open | Disposable restore test is recorded in `docs/live-environment-proof.md`; `docs/launch/evidence/SECRETS_AND_RECOVERY_PROOF.md` refreshes current service/database health and the CLI capability boundary; `docs/launch/evidence/WAF_PROVIDER_POSTURE.md`, Slice 5AV, and Slice 5BH record current deploy/public-edge probes plus the absence of CLI backup/recovery and WAF/rate-limit rule metadata. Slice 5BH reruns `npm.cmd run public-edge:proof`, which reports Cloudflare/Render header presence, selected 404 denials, and common security-header presence without cookies, auth, or response bodies. Render `Production` environment metadata reports `protectedStatus=unprotected`, `networkIsolationEnabled=false`, and IP allow list `0.0.0.0/0`; this is not WAF/rate-limit rule proof. Named owner/deputy, latest recovery-point refresh, and WAF/rate-limit IDs are still not recorded. | Named rollback owner/deputy, database recovery owner, latest recovery point/retention proof, WAF/rate-limit rule IDs, thresholds, protected hostnames, and owner-approved non-destructive test result. |

Owner-proof intake aid: `docs/launch/evidence/P0_OWNER_PROOF_HANDOFF.md` lists the redacted evidence formats needed for the remaining P0 blockers. It is not closure proof by itself.

## Deferred Or Conditional Areas

| Area | Launch Posture | Proof Boundary |
| --- | --- | --- |
| LINE messaging | Optional/manual unless required by owner. | If required, record credentials configured, signed webhook verification, and send-test proof without exposing secrets. |
| OTA automation | iCal/manual metadata only. | No live Booking.com, Agoda, Expedia, Airbnb, or other OTA API/browser automation claim without adapter evidence, challenge handling, dry-run/default safety, and account-owner approval. |
| Payments | PMS-recorded payments only. | No live card, PromptPay, bank-transfer collection, or gateway claim without provider credentials, callback proof, reconciliation proof, and finance owner acceptance. |
| Booking email capture | Setup helper improved, but production capture is not currently working. | Slice 5BI adds `npm.cmd run gmail-oauth:render` so the owner can generate a Google consent URL and exchange a pasted authorization code directly into Render env vars without printing token values. Slice 5BH remains the latest production capture status: `npm.cmd run render:gmail-oauth:status -- --use-render-cli-token` reported `ready=false` for booking-specific and fallback credential paths; the Gmail connector is reachable but not connected to `booking@sandboxhotel.com` and is not a production app-server credential path; `booking-email:proof` job `job-d94460uq1p3s73apai5g` succeeded; dry-run backfill job `job-d9446csvikkc73bh3ba0` failed while Gmail OAuth remained unconfigured. Historical backfill requires backend Gmail OAuth, a redacted dry-run, and review-only import before staff approval in `/booking-inbox`. |

## Proof Standards

- Evidence must be dated and point to a command, provider record, redacted screenshot/export, or manual acceptance note.
- Screenshots and logs must redact tokens, passwords, cookies, raw database URLs, private keys, and customer personal data.
- Local database proof must name the database target and confirm it is disposable or staging before any DB-mutating E2E command.
- Live proof must identify the public target, deploy ID or commit when available, and whether it proves behavior or only infrastructure metadata.
